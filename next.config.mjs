/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'picsum.photos',
      'source.unsplash.com',
      'dummyimage.com',
      'images.pexels.com', // Add this line
    ],
  },
};

export default nextConfig;
